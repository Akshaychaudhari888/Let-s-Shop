version: "3.8"

services:
  backend:
    build: .
    container_name: my-node-app  
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - C:/Users/jalgo/.cache/OneDrive/Desktop/coding:/data/db
