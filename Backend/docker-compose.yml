version: "3.8"

services:
  backend:
    build: .
    container_name: food-delivery-backend
    restart: always
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - uploads_data:/app/uploads

volumes:
  uploads_data:
